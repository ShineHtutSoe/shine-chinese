import { FoundationScreen } from '../components/foundation-screen';
export default function SettingsScreen() { return <FoundationScreen title="Settings" subtitle="App preferences" message="Audio preferences, text size, privacy, account deletion, and offline storage controls will be added after the app foundation is approved." action="Home သို့ ပြန်မယ်" route="/(tabs)" icon="settings" />; }
